[
  {
    "challengeId": "m4-l1-challenge-1",
    "moduleId": "m4",
    "lessonId": "m4-l1",
    "type": "trivia",
    "title": "The exclusion identifier",
    "instructions": "Choose the best description of a nullifier according to the base book.",
    "successCriteria": "Select the option that explains that the nullifier marks an action as performed without revealing identity.",
    "maxAttempts": 3,
    "payload": {
      "question": "What is a nullifier in the book's context?",
      "options": [
        { "id": "a", "label": "A public identifier that shows the voter's identity" },
        { "id": "b", "label": "A value that marks an action as already performed without revealing the citizen's identity" },
        { "id": "c", "label": "A secret key that gets published to the ledger" }
      ],
      "correctOptionId": "b"
    }
  },
  {
    "challengeId": "m4-l2-challenge-1",
    "moduleId": "m4",
    "lessonId": "m4-l2",
    "type": "trivia",
    "title": "Double-vote prevention",
    "instructions": "Identify how the system prevents a citizen from voting twice using nullifiers.",
    "successCriteria": "Select the option explaining that the nullifier is registered and the second attempt is rejected.",
    "maxAttempts": 3,
    "payload": {
      "question": "How does the system prevent double-voting with nullifiers?",
      "options": [
        { "id": "a", "label": "The ledger stores the voter's identity along with their vote" },
        { "id": "b", "label": "The generated nullifier is registered, and the next attempt with the same citizen finds it already exists and is rejected" },
        { "id": "c", "label": "The system limits the number of votes per IP address" }
      ],
      "correctOptionId": "b"
    }
  },
  {
    "challengeId": "m4-l3-challenge-1",
    "moduleId": "m4",
    "lessonId": "m4-l3",
    "type": "trivia",
    "title": "Controlled linking",
    "instructions": "Select the correct formula for generating multiple linked nullifiers from the same user.",
    "successCriteria": "Choose the option using hash(secret_key + nonce).",
    "maxAttempts": 3,
    "payload": {
      "question": "What is the book's formula for generating linked nullifiers with nonce?",
      "options": [
        { "id": "a", "label": "nullifier = hash(secret_key + nonce)" },
        { "id": "b", "label": "nullifier = nonce * 2" },
        { "id": "c", "label": "nullifier = secret_key" }
      ],
      "correctOptionId": "a"
    }
  },
  {
    "challengeId": "m4-l4-challenge-1",
    "moduleId": "m4",
    "lessonId": "m4-l4",
    "type": "trivia",
    "title": "Practical application",
    "instructions": "Identify what the circuit verifies when processing a nullifier.",
    "successCriteria": "Select the option that includes verifying the nullifier was correctly generated and that it doesn't exist in the set.",
    "maxAttempts": 3,
    "payload": {
      "question": "According to the book, what does the circuit verify when processing a nullifier?",
      "options": [
        { "id": "a", "label": "Only verifies the user's identity" },
        { "id": "b", "label": "Verifies the nullifier was correctly generated using the secret key AND that it doesn't yet exist in the ledger's nullifier set" },
        { "id": "c", "label": "Only verifies the nullifier is greater than zero" }
      ],
      "correctOptionId": "b"
    }
  }
]
